#include <iostream>

using namespace std;

int main(int argc, const char *argv[])
{
	double dval;
	int ival;
	int *pi;

	//dval = ival = pi = 0;
	//不能将指针赋给int型
	//修改如下
	dval = ival = 0;
	pi = 0;

	return 0;
}
