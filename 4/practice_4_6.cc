#include <iostream>

using namespace std;

int main()
{
	int i1 = 3;
	int i2 = 6;

	cout << "i1 is: " << (i1 % 2 == 1 ? 1 : 0) << endl;

	cout << "i2 is: " << (i2 % 2 == 1 ? 1 : 0) << endl;

	return 0;
}
