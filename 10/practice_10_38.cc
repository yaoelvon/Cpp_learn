//输入迭代器：只读，不写；单遍扫描，只能递增；还支持相等性判断运算符，解引用运算符和
//箭头运算符
//
//输出迭代器：只写，不读；单遍扫描，只能递增；解引用运算符和
//箭头运算符
//
//前向迭代器：可读写；多遍扫描，可递增，支持所有输入输出迭代器的操作；
//
//双向迭代器：可读写；多遍扫描，可递增递减，并支持所有前向迭代器的操作;
//
//随机访问迭代器：可读写，多遍扫描，支持全部迭代器运算，除了上述迭代器类别支持的操作外，
//还有比较两个迭代器相对位置的关系运算符（<,<=,>,>=）、迭代器和一个整数值的加减运算令
//迭代器在序列中前进和后退给定整数个元素的位置、两个迭代器的减法运算符得到其距离以及
//支持下标运算符；


